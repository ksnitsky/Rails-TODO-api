require "test_helper"

class TodoTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true
  # end

  # seeds = YAML::load_file(Rails.root.join('test', 'fixtures', 'projects.yml'))
  # seeds['projects'].each do |x|
  #   Project.new(
  #     title: x['title'].downcase,
  #     todos: Todo.create(x['todos'])
  #   ).save
  # end
end
