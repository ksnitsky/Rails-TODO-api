require "test_helper"

class ProjectTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true
  # end

  # seeds = YAML::load_file(Rails.root.join('db', 'test.yml'))
  # seeds['Projects'].each do |x|
  #   Project.new(
  #     title: x['title'].downcase,
  #     todos: Todo.create(x['todos'])
  #   ).save
  # end
end
